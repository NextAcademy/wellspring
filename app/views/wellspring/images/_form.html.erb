<%= simple_form_for @image, remote: true, html: { class: "image-upload-form" } do |f| %>
  <br>
  <%= f.input :url, as: :file, label: false %>
  <%= f.submit "Save", class: "btn btn-primary image-create-button" %>
<% end %>